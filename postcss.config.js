// PostCSS config (optional for future Tailwind integration)
// For now, using Sass directly for compilation
// To add Tailwind later:
// 1. npm install -D tailwindcss @tailwindcss/postcss
// 2. Update this config and your build scripts

module.exports = {
  plugins: [
    // require('@tailwindcss/postcss'),
    // require('autoprefixer'),
  ],
}
